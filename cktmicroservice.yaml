kind: Service
apiVersion: v1
metadata:
  name: cktservice
  namespace: microservices
spec:
  selector:
    app: dokerweb
    tier: backend
  type: NodePort
  ports:
  - port: 8181
    targetPort: 8080
    nodePort: 31417
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: cktWeb
  namespace: microservices
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: dokerweb
        tier: backend
        track: stable
    spec:
      containers:
        - name: docerweb
          image: "ultradev/dockerwebv2.0"
          lifecycle:
