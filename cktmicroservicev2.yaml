kind: Service
apiVersion: v1
metadata:
  name: dockerweb
  namespace: microservices
spec:
  selector:
    app: dokerweb
    tier: backend
  ports:
    - protocol: "TCP"
      port: 8181
      targetPort: 8080
  type: NodePort
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: dockerweb
  namespace: microservices
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: dokerweb
        tier: backend
        track: stable
        version : v2
        
    spec:
      containers:
        - name: docerweb
          image: "ultradev/dockerweb:latest"
          lifecycle:
